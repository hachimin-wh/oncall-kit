# oncall-kit 项目分析报告

## 1. 项目定位

**一句话定位**：Anthropic 开源的、面向工程团队值班场景的事故/告警响应 Agent 工具包（插件），搭配 Slack 中的 Claude Tag 或本地 Claude Code 使用。

**核心主张**：Claude 只调查、只提议、只验证，不会改变被监控系统的状态；人负责设定策略、执行修复和关闭事故。经验随事故沉淀，下次调查起点更高。

**个人理解**：**值班 Agent 系统 = oncall-kit + 运行环境**，oncall-kit 是值班业务层设计（行为规则、排查手册与技能、策略与日志模板、评估方法），模型调用、工具执行和任务调度由运行环境承担。团队实践可将其作为定制值班流程的起点，需接入自己的数据和工具、确认策略并完成验证，由团队自行维护和适配；

## 2. 业务场景与职责划分

项目把值班业务为四类场景，各由对应 Skill 承担：第一行是初次的接入配置，后三行是接入完成后的日常运行。

| 使用场景 | 对应 Skill | Claude 承担的工作 | 典型交付 | 人负责的工作 |
|---|---|---|---|---|
| 首次接入团队 | oncall-setup | 探查工具连接；挖掘历史事故起草诊断手册；回放验证 | `STACK.md`、诊断手册草案、`lessons.md`、回放评估结果 | 每阶段签核；设定策略；确认手册与评估 |
| 告警调查与恢复验证 | triage | 取证诊断 → 提修复建议 → 人工修复后复查恢复 | 诊断消息、修复建议、恢复通知（决策记入 `paging-log.md`） | 质询诊断；执行修复；关闭事故 |
| 交班与持续改进 | handoff | 汇总事故与待办；查工具/手册偏差；经验整理为修订 | 交接消息、偏差提示、手册修订 PR | 审阅交接；决定偏差处置；合并 PR |
| 状态查询与定期报告 | 按需问答；weather（可选） | 回答状态提问；定期更新报告页，仅事件门触发时通知 | 状态答复、报告页、事件提醒 | 在 `ONCALL.md` 设定报告策略；收到提醒后判断处置 |

四个场景都是同一个模式：**Claude 产出带证据的判断，人做决定和动作**；Claude的交付物全是文字——频道消息和日志文件，没有任何系统操作。

## 3. 项目结构与架构

### 3.1 项目结构与模块职责

```text
oncall-kit/
├── CLAUDE.md                   常设行为规则
├── skills/
│   ├── oncall-setup/            五阶段接入流程
│   ├── triage/                 告警调查流程
│   │   └── references/         按故障类别组织的排查手册
│   ├── handoff/                周交接与工具、手册失效检查
│   └── weather/                可选状态报告
├── templates/                  策略、能力映射、日志与例程模板
├── eval/replay.md              回放评估与试运行标准
├── test-fixtures/              虚构事故与回归验收材料
├── examples/run1-webshop/      虚构电商团队示例
└── .claude-plugin/ + hooks/    插件声明与首次启动提示
```

仓库自带的文件分两类：一类是**直接生效的业务方法**：`CLAUDE.md` 行为规则、`skills/` 的任务流程与排查手册、`eval/` 评估协议，接入后原样使用；另一类是 **`templates/` 模板**，需结合团队实际实例化。首次接入时，Claude 依据模板生成团队自己的工作文件：`STACK.md`、`ONCALL.md`、`lessons.md`、`paging-log.md`，以及告警覆盖报告和回放评估结果；

| 模块 | 主要职责 | 本项目中的位置与例子 |
|---|---|---|
| 常设规则 | 约束 Agent 行为、证据与人工分工 | `CLAUDE.md`：不执行修复、不关闭事故；诊断附证据和置信度；外部记录作为数据处理。 |
| 团队策略 | 决定严重度、通知阈值、负责人及升级路径 | `ONCALL.md` 模板：由团队确认策略，落地后保存在仓库根目录。 |
| Skill 与排查手册 | 规定一项任务怎么做，以及某类故障查什么 | oncall-setup、triage、handoff、weather 四个 Skill 分别对应第 2 节的四类场景；`triage/references/` 提供分类检查项、原因线索和升级条件，默认手册面向 CI 场景，迁移到其他业务需替换。 |
| 工具能力映射 | 指明去哪查数据，记录已验证的访问能力与缺口 | `STACK.md` 模板：将指标、日志、代码等八类能力映射到实际工具。它记录连接结果，本身不安装 MCP 服务或授予权限。 |
| Routine（频道例程） | 指定何时、在哪个频道执行任务 | `routines.md`：提供监听告警、每周交接等任务文本，由人填入实际频道与时间并启用，运行平台负责触发。 |
| 经验与审计 | 保留调查知识和通知决策依据 | `lessons.md`：按故障标签积累和检索经验；`paging-log.md`：记录是否发出紧急通知的依据、观测值和升级过程，按月轮转。 |
| 插件安装信息与启动钩子 | 登记插件，在尚未配置时提示用户开始接入 | `plugin.json` 描述插件；`first-run.sh` 检查配置文件是否存在并输出提示，不自动探查连接或启用任务。 |
| 评估与示例 | 定义验收方法，提供演练材料 | `eval/replay.md` 定义回放与试运行标准；`test-fixtures/` 提供 48 起虚构事故；电商示例 `examples/run1-webshop/` 展示非 CI 团队如何适配。 |

### 3.2 架构
```mermaid
flowchart TB
    ALERT["现有告警系统"]
    HUMAN["值班人员、服务负责人<br/>确认方案、执行修复、关闭事故"]
    CHANNEL["Slack 频道<br/>告警、请求、诊断与进展"]

    subgraph HOST["Claude Tag：执行与调度"]
        ROUTINE["频道例程<br/>事件触发 / 定时触发"]
        AGENT["Claude 执行会话<br/>读取文件、调查、诊断、验证"]
        TOOLS["已接入的工具连接器<br/>提供外部系统访问能力"]
    end

    subgraph REPO["接入 oncall-kit 的团队仓库"]
        RULE["CLAUDE.md + ONCALL.md<br/>行为边界、分级与通知策略"]
        SKILL["skills/ + triage/references/<br/>任务流程与分类排查手册"]
        STACK["STACK.md<br/>数据能力到工具的映射"]
        LOG["lessons.md + paging-log.md<br/>事故经验与通知决策审计"]
    end

    SYSTEM["团队外部系统<br/>监控、日志、代码、发布、事故平台"]
    ALERT -->|告警消息| CHANNEL
    ALERT -->|紧急告警直接通知| HUMAN
    CHANNEL -->|新告警事件| ROUTINE
    ROUTINE -->|触发任务| AGENT
    CHANNEL -->|人工请求| AGENT
    RULE -->|约束执行| AGENT
    SKILL -->|指导调查与汇报| AGENT
    STACK -->|指明使用哪个工具| AGENT
    AGENT <-->|读取经验、追加记录| LOG
    AGENT <-->|工具调用与结果| TOOLS
    TOOLS <-->|访问已授权数据与接口| SYSTEM
    AGENT -->|诊断、建议与验证结果| CHANNEL
    CHANNEL <-->|确认与反馈| HUMAN
    HUMAN -->|执行修复| SYSTEM
```
**Slack 作为 IM 与协作入口，oncall-kit 提供规则与方法，Claude Tag 负责调度和执行任务。** 告警进入 Slack 后，Claude Tag 根据配置的监听规则启动调查任务；Claude 读取 oncall-kit 中的策略、排查手册和历史经验，依据 `STACK.md` 的工具映射，查询指标、日志和变更记录，在频道中输出带证据的诊断与修复建议。事故响应人员确认并执行修复后，Claude 复查原始故障信号、同步恢复情况并记录经验；事故由人工确认关闭。紧急告警同时通过原有告警机制直接通知人员，不依赖 Agent 的调查结果。

### 3.3 运行案例：告警调查与恢复验证

以下是“MySQL 内存告警”的假设案例，用于说明协作过程；仓库未预置该类手册，需先接入数据库观测工具并适配检查步骤。

```mermaid
sequenceDiagram
    participant S as Slack 告警频道
    participant R as 宿主 / 频道例程
    participant C as Claude
    participant T as 数据库观测等工具
    participant H as 事故响应人员

    Note over S,H: 紧急告警通过原有机制直接通知人员
    S->>R: MySQL 内存告警到达
    R->>C: 触发任务，要求执行 triage
    C->>C: 读取 triage/SKILL.md
    Note over C: 遵循 CLAUDE.md<br/>读取 ONCALL.md、STACK.md<br/>匹配 references/，检索 lessons.md
    C->>T: 查询指标、慢 SQL 和发布记录
    T-->>C: 返回观测数据与证据
    C->>S: 发布诊断、证据与处理建议
    S-->>H: 查看诊断与建议
    H->>H: 人工确认并执行操作
    H->>C: 反馈处理结果
    C->>T: 复查原始告警信号与业务影响
    T-->>C: 返回验证结果
    C->>S: 同步恢复情况或按策略升级
    Note over C,H: 事故由人工确认关闭
    C->>C: 记录经验，按需提出手册修订 PR
```

假设观测显示：新报表发布后，并发查询量与内存占用同时上升。Claude 需要继续核对时间线和影响范围，排除其他原因，再给出带证据的判断与处理建议。人执行措施后，Claude 复查原始内存信号及业务影响，报告是否恢复。每次紧急通知决策都需单独记录依据，事故最终由人关闭。

### 3.4 验证机制

上线前后各有一道检查：

- **上线前 · 回放盲测**：从历史事故里挑 5–10 起没参与编写手册的，只给当时的告警消息，让 Claude 按诊断手册跑模拟调查。人工确认诊断是否过线（标准：正确+部分正确 ≥70% 且零有害诊断，模板预设标准），不通过就修改手册、换一批事故重测。
- **上线后 · 试运行**：上线后不会直接协助告警处理，只将诊断输出到审查频道供人评分，事故处理人照常处理事故。连续 10 次诊断被判定有帮助且无害，才正式上线、进入真实告警频道输出诊断与通知；满两周未达标则复核，决定延长、修复还是放弃。

## 4. 值得借鉴的设计范式

oncall-kit 的核心思路是：**用文件承载业务方法，让 Agent 在明确边界内调查，再把经过验证的经验沉淀下来。**

| 设计范式 | 项目中的体现 | 可复用价值 |
|---|---|---|
| **规则与知识外置** | 将行为约束、团队策略、排查手册和经验分别存成文件，执行任务时读取，以文件为准。 | 将业务方法从临时对话中独立出来，便于维护、复用和跨会话延续。 |
| **能力与工具解耦** | 手册只描述“查指标、查日志”等能力，`STACK.md` 将能力映射到实际工具。 | 把调查方法与工具差异分开，迁移到其他团队时可保留流程、适配工具。 |
| **按风险划分自主权** | 原有系统负责告警，Agent 负责调查、建议和验证，人负责执行修复与关闭事故。 | 将模型自主性用于开放式分析，把高影响动作留给人，明确自动化边界。 |
| **以证据约束判断** | 诊断附证据、置信度和可推翻结论的条件；恢复验证重查原始故障信号。 | 让结论可质询、可复核，明确区分事实、假设和未知，减少无依据的判断。 |
| **经验与正式规则分层** | 自动记录事故经验；将重复出现或已形成可复用步骤的经验，经人工确认纳入手册，并通过回放检验。 | 让经验快速积累、规则审慎更新，形成“调查→记录→提炼→验证”的持续改进闭环。 |
